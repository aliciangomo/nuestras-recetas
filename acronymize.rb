describe("acronymize") do
  it ("returns an empty string when passed an empty string") do
    actual = acronimize("")
    expected = ""
    expect(actual).to eq(expected)
  end

  it ("requires an...")
    actual = ...
    expected = ...
    expect(actual).to eq(expected)
  end



In terminal run rspec filename.rb
